# CLAUDE.md

## 프로젝트 핵심
- Project Plateau는 PDF 기획 논문의 진화형 AI/탑뷰 생존 전투 방향을 검증하는 전체화면 웹 프로토타입이다.
- 현재 구현은 TypeScript와 Three.js CDN 모듈 기반 3D 렌더링이며, 클릭 이동/자연물 상호작용/좌측 하단 전투 액션창을 포함한다.
- 카메라는 듀랑고풍 탑뷰를 유지하되 살짝 기울여 플레이어, 랩터, 지형의 3D 돌출감을 보이게 한다.
- 외부 에셋이 없어도 빌드되도록 플레이어/랩터/지형은 골격이 보이는 코드 기반 프록시 모델을 우선 사용한다.
- 플레이어와 랩터 프록시 모델은 관절·손발가락·이빨·눈·근육 가중치 블록을 분리해 실제 에셋 교체 전 해부학적 구조를 검증한다.

## 개발 규칙
- 코드에는 다음 작업자가 의도를 이해할 수 있는 한국어 주석을 남긴다.
- 시각 테마는 어두운 고원 배경과 녹색/모래색 하이라이트를 유지하되, 현재 화면에는 부가 설명 UI를 두지 않는다.
- 실제 에셋을 추가할 때는 라이선스와 출처를 문서화하고, 빌드 실패를 막기 위해 대체 프록시를 유지한다.
- CDN Three.js 버전을 바꿀 때는 `src/three-remote.d.ts`의 타입 shim도 함께 갱신한다.

## 주요 명령
- `npm run dev`: 로컬 정적 서버 실행. 최초 실행 전 `npm run build`로 `build/main.js`를 생성한다.
- `npm run typecheck`: TypeScript 타입 검사.
- `npm run build`: TypeScript 컴파일 후 정적 배포 산출물 생성.
